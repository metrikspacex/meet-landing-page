import type { Routes } from "@angular/router";

import { HomeComponent } from "./pages/home/home.component";

const routes: Routes = [
  {
    component: HomeComponent,
    path: "",
    title: "Frontend Mentor | Meet landing pag",
  },
];
export default routes;
